# RealmOS / Jarvis HQ — Package Index

## Active Cursor Source of Truth

The active Cursor control file is:

```text
CURSOR_SSOT.md
```

First Cursor prompt:

```text
Read CURSOR_SSOT.md and follow it exactly. Do Phase 0 only. Stop after the Phase 0 report and do not start implementation until I approve.
```

## Current Repo Structure

```text
realmos/
  CURSOR_SSOT.md
  PROJECT_STATE.md
  SSOT_TODO_CHECKLIST.md
  specs/realmos-mvp/
  apps/api/
  apps/worker/
  packages/contracts/
  packages/core/
  packages/agents/
  packages/governance/
  packages/memory/
  packages/tools/
  packages/llm-router/
  packages/ui/
  docs/realmos-package/
  assets/ui-references/
```

## Build Order

Use `CURSOR_SSOT.md` and `SSOT_TODO_CHECKLIST.md` for the active build order.

Do not use older package read-order documents as active instructions.
