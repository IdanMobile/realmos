{
  "name": "@realmos/fleet-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo \"lint fleet-control\""
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "^5.8.0",
    "vitest": "latest"
  }
}
