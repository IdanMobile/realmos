{
  "name": "@realmos/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "echo \"lint ui\""
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "latest",
    "@types/node": "latest"
  }
}
